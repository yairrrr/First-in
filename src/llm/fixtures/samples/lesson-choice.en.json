{
  "concept": "The code uses a guard clause at the beginning to skip the rest of the function. This prevents the game from processing a card click if the game is currently busy or the card is already finished.",
  "example": "if (!canClick || cardElement.classList.contains('flipped') || cardElement.classList.contains('matched')) {\n                return;\n            }",
  "question": "What happens if the user clicks a card that is already flipped?",
  "options": [
    "The card stays flipped.",
    "The move count increases.",
    "The code runs but skips everything.",
    "The game resets automatically."
  ],
  "correctIndex": 2
}
